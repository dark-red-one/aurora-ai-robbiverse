# Redis Sentinel Configuration
# Prevents split-brain scenarios in Aurora mesh

port 26379
sentinel deny-scripts-reconfig yes

# Monitor Aurora Redis instance
sentinel monitor aurora-redis 172.20.0.10 6379 2
sentinel auth-pass aurora-redis ${REDIS_PASSWORD}
sentinel down-after-milliseconds aurora-redis 5000
sentinel parallel-syncs aurora-redis 1
sentinel failover-timeout aurora-redis 10000

# Sentinel configuration
sentinel myid ${SENTINEL_ID:-aurora-sentinel-001}
sentinel announce-ip ${SENTINEL_IP:-172.20.0.11}
sentinel announce-port 26379

# Logging
loglevel notice
logfile ""

# Security
protected-mode no
