#!/usr/bin/env node
import { spawn } from "child_process";
import path from "path";
const entry = path.resolve("/home/allan/vengeance/src/index.js");
const proc = spawn(process.execPath, [entry], { stdio: "inherit" });
proc.on("exit", (code) => process.exit(code ?? 0));


